--- decompiled/JuliEditor/JuliEditor/FileManager.cs
+++ src/JuliEditor/JuliEditor/FileManager.cs
@@ -5,7 +5,6 @@
 using System.Linq;
 using System.Runtime.InteropServices;
 using System.Text;
-using System.Windows.Forms;
 using JuliHelper;
 using JuliHelper.Camera;
 
@@ -44,6 +43,7 @@
 
 	public static Level Open(Camera camera)
 	{
+#if WPF_LEGACY
 		OpenFileDialog openFileDialog = new OpenFileDialog();
 		string[] array = writers.Keys.ToArray();
 		string text = filterStart;
@@ -57,6 +57,7 @@
 		{
 			return OpenFromPath(openFileDialog.FileName, camera);
 		}
+#endif
 		return null;
 	}
 
@@ -82,6 +83,7 @@
 
 	public static bool SaveAs(Level level)
 	{
+#if WPF_LEGACY
 		SaveFileDialog saveFileDialog = new SaveFileDialog();
 		saveFileDialog.FileName = Path.GetFileNameWithoutExtension(level.Path) + ".lvl";
 		saveFileDialog.Filter = filterStart + customFilter + filterEnd;
@@ -90,6 +92,7 @@
 			SaveToPath(level, saveFileDialog.FileName);
 			return true;
 		}
+#endif
 		return false;
 	}
 
